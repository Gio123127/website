import { PagePlaceholder } from "@/components/page-placeholder";

export default function HomePage() {
  return (
    <PagePlaceholder
      eyebrow="Hastings · Hawke's Bay"
      title="Beautiful nails & lashes — never rushed."
      description="One artist, your full attention, in a private Hastings studio. The full site is coming together — book your appointment any time."
    />
  );
}
